# Production requirements for Vercel Python serverless (python3.12)
# Pin versions to ensure fast, reproducible builds on Vercel.
# uvicorn is NOT included — Vercel calls the ASGI app directly.

fastapi==0.115.0
pandas==2.2.3
numpy==2.1.3
pydantic==2.10.0
python-dotenv==1.0.1
google-genai==1.14.0
scikit-learn==1.5.2
python-multipart==0.0.12
anyio==4.6.0
starlette==0.41.2
