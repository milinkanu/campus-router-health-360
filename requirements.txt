# Production requirements for Vercel Python serverless (python3.12)
# Only pin DIRECT dependencies. Let uv/pip resolve transitive deps
# (starlette, anyio, httpcore, etc.) to avoid version conflicts.

fastapi>=0.110.0,<0.116.0
pandas>=2.2.0,<3.0.0
numpy>=1.26.0,<3.0.0
pydantic>=2.6.0,<3.0.0
python-dotenv>=1.0.0
google-genai>=2.0.0
scikit-learn>=1.4.0,<2.0.0
python-multipart>=0.0.9
